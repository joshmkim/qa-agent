# Control plane base URL. When set, pages read live data from it.
CONTROL_PLANE_URL=http://localhost:3001
# Force a data source: "mock" (fixtures) or "api" (control plane).
# Unset: api when CONTROL_PLANE_URL is set, otherwise mock.
DATA_SOURCE=
